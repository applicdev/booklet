{
  // ? "./lib" and "./lib/worker" deno .ts files, other
  //   files are expected to be DOM and WebWorker
  "deno.lint": true,
  "deno.enable": true,
  "deno.enablePaths": ["./lib/worker/**/*.ts", "./lib/*.ts"]
}
