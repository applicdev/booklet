{
  // ? "./lib/*.ts" and "./lib/worker/**/*.ts " are considered Deno files, any
  //   other typescript files are considered for use as Deno, DOM and WebWorker
  "deno.enable": true,
  "deno.enablePaths": ["./lib/worker/**/*.ts", "./lib/*.ts"]
}
